# API Test Agent Skill

## Commands

- `/api-test-generate -i <openapi_path> -o <output_dir>`
  - 等价：`python cli.py generate -i <openapi_path> -o <output_dir>`

- `/api-test-heal -t <tests_dir>`
  - 等价：`python cli.py heal -t <tests_dir>`

